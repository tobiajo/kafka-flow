\* spec: RecoveryRead
\* expect: VIOLATES INV_ReadsAllCommitted
\* flags: -deadlock
\* The under-read (finding F-10), as first written: unique per-assignment transactional.ids (nobody
\* aborts crashed A's open transaction) and the read bounded at its own read_committed endOffsets --
\* the LSO, pinned below B's committed S3. The read completes early, silently missing S3; with the
\* input offset already committed past it, the #732 corruption shape with no fence violated.
\* Replicated live: an 85 ms "successful" read missing a committed snapshot.
SPECIFICATION Spec
CONSTANTS StableId = FALSE  HwTarget = FALSE  Foreign = FALSE
INVARIANT TypeOK
INVARIANT INV_ReadCommittedOnly
INVARIANT INV_ReadsAllCommitted
