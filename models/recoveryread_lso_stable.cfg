\* spec: RecoveryRead
\* expect: HOLDS
\* flags: -deadlock
\* Remedy (2), the adopted design: a stable per-partition transactional.id. B's MANDATORY
\* initTransactions aborts A's open transaction before B writes S3, so within the lineage a committed
\* record above an open transaction is unreachable (INV_LineageSerialized) -- the plain read_committed
\* endOffsets bound is complete with NO wait and NO reader-side ordering assumption: C completes at
\* B's own dangling transaction without waiting it out, missing nothing (Terminates holds with no
\* TimeoutAbort needed on the completion path).
SPECIFICATION Spec
CONSTANTS StableId = TRUE  HwTarget = FALSE  Foreign = FALSE
INVARIANT TypeOK
INVARIANT INV_ReadCommittedOnly
INVARIANT INV_ReadsAllCommitted
INVARIANT INV_LineageSerialized
PROPERTY Terminates
